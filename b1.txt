create database student_management;
use student_management;
create table class(
id int primary key,
name varchar(45)
);
Create table teacher(
id int primary Key,
name varchar(45),
age int,
country varchar(45)
);